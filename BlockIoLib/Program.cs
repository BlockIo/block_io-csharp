using BlockIoLib.Examples;
using System;

namespace BlockIoLib
{
    class Program
    {
        static void Main(string[] args)
        {
            /**
             * Examples
             */

            // DTrust dTrustExample = new DTrust();
            // dTrustExample.RunDtrustExample();

            //string api_key = "0000-0000-0000-0000";
            //string pin = "blockiotestpininsecure";
            //var port = new Random().Next(5000, 6000);
            //string baseUrl = "http://localhost:" + port + "/api/v2";
            //BlockIo blockIo = new BlockIo(api_key, pin, 2, "{api_url: '" + baseUrl + "'}");

        }
    }
}
